# ============================================
# MaxOps Nginx 서버 설정
# ============================================

# ------------------------------------------
# 업스트림 서버 정의
# ------------------------------------------
upstream openproject_upstream {
    server openproject:8080;
    keepalive 32;
}

upstream gitea_upstream {
    server gitea:3000;
    keepalive 32;
}

# ------------------------------------------
# 메인 서버 (포트 80)
# ------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    # 문자셋
    charset utf-8;

    # 클라이언트 업로드 크기
    client_max_body_size 512m;

    # ------------------------------------------
    # 루트 경로 - 서비스 선택 페이지
    # ------------------------------------------
    location = / {
        default_type text/html;
        return 200 '<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MaxOps - 프로젝트 관리 시스템</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
        }
        .container { text-align: center; padding: 2rem; }
        h1 {
            font-size: 3rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(90deg, #00d2ff, #3a7bd5);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .subtitle { color: #888; margin-bottom: 3rem; }
        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            max-width: 800px;
        }
        .service-card {
            background: rgba(255,255,255,0.05);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 16px;
            padding: 2rem;
            transition: all 0.3s ease;
            text-decoration: none;
            color: inherit;
        }
        .service-card:hover {
            transform: translateY(-5px);
            border-color: #3a7bd5;
            box-shadow: 0 10px 40px rgba(58,123,213,0.2);
        }
        .service-icon { font-size: 3rem; margin-bottom: 1rem; }
        .service-title { font-size: 1.5rem; margin-bottom: 0.5rem; }
        .service-desc { color: #888; font-size: 0.9rem; }
        .service-port {
            display: inline-block;
            margin-top: 1rem;
            padding: 0.25rem 0.75rem;
            background: rgba(58,123,213,0.2);
            border-radius: 20px;
            font-size: 0.8rem;
            color: #3a7bd5;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>MaxOps</h1>
        <p class="subtitle">통합 프로젝트 관리 시스템</p>
        <div class="services">
            <a href="/op/" class="service-card">
                <div class="service-icon">📊</div>
                <div class="service-title">OpenProject</div>
                <div class="service-desc">일정 관리, Task 관리, 문서 관리</div>
                <span class="service-port">admin / admin</span>
            </a>
            <a href="/git/" class="service-card">
                <div class="service-icon">🔀</div>
                <div class="service-title">Gitea</div>
                <div class="service-desc">소스코드 저장소, CI/CD</div>
                <span class="service-port">최초 접속시 계정 생성</span>
            </a>
        </div>
    </div>
</body>
</html>';
    }

    # ------------------------------------------
    # OpenProject 프록시 (/op/)
    # ------------------------------------------
    location /op/ {
        proxy_pass http://openproject_upstream/;
        proxy_http_version 1.1;

        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
    }

    # ------------------------------------------
    # Gitea 프록시 (/git/)
    # ------------------------------------------
    location /git/ {
        proxy_pass http://gitea_upstream/;
        proxy_http_version 1.1;

        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        client_max_body_size 512m;
    }

    # ------------------------------------------
    # 상태 확인 엔드포인트
    # ------------------------------------------
    location /health {
        access_log off;
        default_type application/json;
        return 200 '{"status":"healthy"}';
    }

    # ------------------------------------------
    # 에러 페이지
    # ------------------------------------------
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        default_type text/html;
        return 500 '<!DOCTYPE html>
<html><head><meta charset="UTF-8"><title>오류</title>
<style>body{font-family:sans-serif;background:#1a1a2e;color:#fff;display:flex;align-items:center;justify-content:center;min-height:100vh;margin:0}
h1{color:#ff6b6b}p{color:#888}</style></head>
<body><div><h1>서버 오류</h1><p>잠시 후 다시 시도해 주세요.</p></div></body></html>';
    }
}
